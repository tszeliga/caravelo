// Asset path constants and aliases

// Style paths
export const STYLE_PATHS = {
  GLOBALS: '@styles/globals.scss'
} as const

// Common asset paths (when we add images, fonts, etc.)
export const ASSET_PATHS = {
  STYLES: '@styles',
  IMAGES: '@assets/images',
  FONTS: '@assets/fonts',
  ICONS: '@assets/icons'
} as const

